# Architecture Tests

Static checks parse authored Python imports without importing application modules. They enforce layer direction, prohibit direct Neo4j imports outside infrastructure, and detect package dependency cycles.
