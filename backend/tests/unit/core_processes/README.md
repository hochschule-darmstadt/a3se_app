# Core Process Unit Tests

Unit evidence for core-process capabilities, including the grounded Customer
Care advisor service and the AI travel agent. Travel-intent tests replace the
local model with scripted extractors, so they run offline.
