# Python Source Root

Only installable production packages live below this `src` root. Tests remain in `backend/tests` so a source checkout cannot accidentally make test modules part of the runtime distribution.
