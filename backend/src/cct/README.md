# CCT Python Package

`cct` is the explicit backend import namespace for Christopher Columbus Travel. It prevents top-level imports such as `api`, `core_processes`, or `resource_management` from colliding with third-party or unrelated code.
