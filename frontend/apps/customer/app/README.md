# Customer Application Source

`root.tsx` owns the application shell and design-system provider. `routes.ts` is the navigation manifest. Feature route modules live under `routes/` and remain adapters over shared HTTP contracts.
