# Customer Route Helpers

Pure, framework-independent helper modules (product display formatting, stock-availability lookup, order-submission sequencing) shared by more than one route. Route-specific logic stays in the route module; only genuinely reusable, testable-in-isolation logic lives here.
