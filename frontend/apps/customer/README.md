# Customer Interaction

React Router Framework Mode application for MOD-CI. It is configured as a client-rendered SPA. Route modules own customer navigation and presentation only; they call backend capabilities through `@cct/api-client` and do not contain business or persistence rules.

The scaffold deliberately contains one placeholder route. Customer features and tests belong to their implementing issue.
