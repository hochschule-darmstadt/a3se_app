# Applications

This directory contains independently runnable CCT frontend applications. The
`customer` and `staff` applications share technical building blocks through
the sibling `packages` workspace while retaining separate routes and releases.

