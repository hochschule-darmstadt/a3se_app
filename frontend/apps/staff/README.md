# Staff Interaction

React Router Framework Mode application for MOD-SI. It is a client-rendered SPA with a compact design profile. Route modules call the same capability-oriented HTTP contracts as Customer Interaction; the backend API is not partitioned by frontend.

The scaffold deliberately contains one placeholder route. Staff features and tests belong to their implementing issue.
