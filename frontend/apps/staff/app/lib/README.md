# Staff Route Helpers

Pure, framework-independent helper modules (the shared navigation shell, cursor-pagination state) shared by more than one route. Route-specific logic stays in the route module; only genuinely reusable, testable-in-isolation logic lives here.
