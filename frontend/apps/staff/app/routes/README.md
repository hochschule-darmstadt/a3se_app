# Staff Routes

Route modules are grouped by staff-facing feature as the application grows. List/filter state should use the URL when appropriate and every route must retain keyboard and error-state behavior.
