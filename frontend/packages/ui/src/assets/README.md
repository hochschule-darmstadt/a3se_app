# UI assets

Shared, presentation-only assets consumed by `@cct/ui`. Keep authoritative
design sources and their review evidence under `docs/requirements/ux/`; this
directory contains only the application-ready copies.
