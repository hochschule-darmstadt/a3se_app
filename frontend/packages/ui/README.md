# Shared UI Foundation

This package implements shared semantic theme foundations from the accepted design system. Customer and Staff profiles share tokens and accessible primitives while retaining different branding/density. Feature-specific components remain in their owning application until reuse is proven.
