# Shared frontend packages

This directory contains framework-neutral API access and shared presentation
foundations used by both frontend applications. Business features remain in
the application that owns their user interaction until reuse is demonstrated.

