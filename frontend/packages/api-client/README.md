# Shared API Client Boundary

This package is the only frontend package intended to expose backend HTTP contracts. Issue #21 will generate or validate the concrete TypeScript client from FastAPI OpenAPI. The scaffold exports configuration types only and contains no handwritten persistence or business models.
